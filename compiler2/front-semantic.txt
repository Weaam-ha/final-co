<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Product List</title>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet">

    <style>
        *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

        :root {
            --cream:    #f0f4fa;
            --sand:     #dde6f5;
            --bark:     #89aad4;
            --soil:     #3a6399;
            --ink:      #0f1e35;
            --mist:     #ffffff;
            --accent:   #2563b0;
            --accent2:  #4a90d9;
            --shadow:   rgba(15,30,53,0.10);
            --radius:   18px;
            --danger:   #e04f4f;
            --danger-hover: #c93b3b;
        }

        body {
            background: var(--cream);
            font-family: 'DM Sans', sans-serif;
            color: var(--ink);
            min-height: 100vh;
            background-image:
                radial-gradient(ellipse 80% 60% at 10% 0%, #dce8f7 0%, transparent 60%),
                radial-gradient(ellipse 60% 50% at 90% 100%, #d8e8f5 0%, transparent 55%);
        }

        .container {
            display: flex;
            gap: 28px;
            width: 92%;
            max-width: 1280px;
            margin: 48px auto;
            min-height: 82vh;
            align-items: flex-start;
        }

        .card {
            background: var(--mist);
            border-radius: var(--radius);
            box-shadow: 0 2px 0 var(--sand), 0 12px 40px var(--shadow);
            border: 1px solid var(--sand);
        }

        .product-list {
            flex: 0 0 340px;
            padding: 32px 28px 28px;
        }

        .product-list h3 {
            font-family: 'DM Serif Display', serif;
            font-size: 26px;
            font-weight: 400;
            color: var(--ink);
            margin-bottom: 22px;
            padding-bottom: 14px;
            border-bottom: 1.5px solid var(--sand);
            letter-spacing: -0.3px;
        }

        .product-list ul { list-style: none; }

        .product-list li {
            display: flex;
            align-items: center;
            gap: 14px;
            padding: 13px 14px;
            border-radius: 12px;
            margin-bottom: 8px;
            cursor: pointer;
            transition: background 0.18s, transform 0.18s, box-shadow 0.18s;
            border: 1px solid transparent;
        }

        .product-list li:hover {
            background: var(--cream);
            border-color: var(--bark);
            transform: translateX(4px);
            box-shadow: 0 4px 14px var(--shadow);
        }

        .product-list li img {
            width: 52px;
            height: 52px;
            border-radius: 10px;
            object-fit: cover;
            flex-shrink: 0;
            border: 1px solid var(--sand);
        }

        .product-list li span {
            font-size: 15px;
            font-weight: 500;
            color: var(--soil);
            line-height: 1.3;
        }

        .add-section { margin-top: 30px; }

        .add-section h3 {
            font-family: 'DM Serif Display', serif;
            font-size: 20px;
            font-weight: 400;
            color: var(--ink);
            margin-bottom: 16px;
            padding-bottom: 10px;
            border-bottom: 1.5px solid var(--sand);
        }

        .add-section form {
            display: flex;
            flex-direction: column;
            gap: 11px;
        }

        .add-section input {
            width: 100%;
            padding: 11px 14px;
            border-radius: 10px;
            border: 1.5px solid var(--sand);
            background: var(--cream);
            font-family: 'DM Sans', sans-serif;
            font-size: 14px;
            color: var(--ink);
            transition: border-color 0.18s, box-shadow 0.18s;
            outline: none;
        }

        .add-section input::placeholder { color: var(--bark); }

        .add-section input[type="number"]::-webkit-inner-spin-button,
        .add-section input[type="number"]::-webkit-outer-spin-button { -webkit-appearance: none; margin: 0; }
        .add-section input[type="number"] { -moz-appearance: textfield; }

        .add-section input:focus {
            border-color: var(--accent);
            background: #fff;
            box-shadow: 0 0 0 3px rgba(212,118,59,0.12);
        }

        .add-section button {
            padding: 12px;
            background: var(--accent);
            color: #fff;
            border: none;
            border-radius: 10px;
            font-family: 'DM Sans', sans-serif;
            font-size: 15px;
            font-weight: 500;
            cursor: pointer;
            transition: background 0.18s, transform 0.15s, box-shadow 0.18s;
            letter-spacing: 0.2px;
            margin-top: 2px;
        }

        .add-section button:hover {
            background: var(--soil);
            transform: translateY(-2px);
            box-shadow: 0 6px 18px rgba(122,92,62,0.22);
        }

        .product-details {
            flex: 1;
            padding: 44px 48px;
            min-height: 520px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
        }

        .product-details h3 {
            font-family: 'DM Serif Display', serif;
            font-size: 32px;
            font-weight: 400;
            color: var(--ink);
            margin-bottom: 28px;
            letter-spacing: -0.5px;
        }

        .product-details img {
            width: 280px;
            height: 280px;
            object-fit: cover;
            border-radius: 20px;
            margin-bottom: 30px;
            box-shadow: 0 2px 0 var(--sand), 0 18px 48px var(--shadow);
            border: 1px solid var(--sand);
        }

        .detail-row {
            display: flex;
            align-items: center;
            gap: 10px;
            margin: 10px 0;
            font-size: 17px;
        }

        .detail-label {
            font-size: 11px;
            font-weight: 500;
            letter-spacing: 1.2px;
            text-transform: uppercase;
            color: var(--bark);
            background: var(--cream);
            border: 1px solid var(--sand);
            border-radius: 6px;
            padding: 3px 9px;
        }

        .detail-value {
            color: var(--ink);
            font-weight: 400;
        }

        .btn-delete {
            display: inline-block;
            margin-top: 25px;
            padding: 10px 24px;
            background: var(--danger);
            color: white;
            border: none;
            border-radius: 10px;
            font-family: 'DM Sans', sans-serif;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: background 0.18s, transform 0.15s;
        }

        .btn-delete:hover {
            background: var(--danger-hover);
            transform: translateY(-2px);
        }

        .empty-state {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 14px;
            opacity: 0.55;
        }

        .empty-icon {
            width: 72px;
            height: 72px;
            border-radius: 50%;
            background: var(--sand);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
        }

        .empty-state p {
            font-family: 'DM Serif Display', serif;
            font-size: 20px;
            color: var(--soil);
        }

        .product-list { overflow-y: auto; max-height: 90vh; }
        .product-list::-webkit-scrollbar { width: 4px; }
        .product-list::-webkit-scrollbar-track { background: transparent; }
        .product-list::-webkit-scrollbar-thumb { background: var(--bark); border-radius: 4px; }

        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(16px); }
            to   { opacity: 1; transform: translateY(0); }
        }
        .product-list  { animation: fadeUp 0.45s ease both; }
        .product-details { animation: fadeUp 0.45s 0.1s ease both; }
    </style>
</head>

<body>
    <div class="container">

        <div class="product-list card">
            <h3>Products</h3>
////semantic
   <ul>
                {% for product in products %}
                <li onclick="window.location.href='/product/{{ product.id }}'">
                    <img src="{{ product.image }}" alt="{{ product.name }}">
                    <span>{{ product.name }}</span>
                </li>
                {% endfor %}
            </ul>
/////////////////
            <ul>
                {% for product in 5 %}
                <li onclick="window.location.href='/product/{{ product.id }}'">
                    <img src="{{ product.image }}" alt="{{ product.name }}">
                    <span>{{ product.name }}</span>
                </li>
                {% endfor %}
                {{ product.name }}
                {{i}}
                  {{ "text" + 1 }}
            </ul>

            <div class="add-section">
                <h3>Add Product</h3>
                <form action="/add" method="POST">
                    <input type="text"   name="name"    placeholder="Product name"  required>
                    <input type="text"   name="details" placeholder="Details"       required>
                    <input type="number" name="price"   placeholder="Price ($)"     required>
                    <input type="text"   name="image"   placeholder="Image URL">
                    <button type="submit">Add Product</button>
                </form>
            </div>
        </div>

        <div class="product-details card">
            {% if selected_product %}
                <h3>{{ selected_product.name }}</h3>

                <img src="{{ selected_product.image }}" alt="{{ selected_product.name }}">

                <div class="detail-row">
                    <span class="detail-label">Details</span>
                    <span class="detail-value">{{ selected_product.details }}</span>
                </div>

                <div class="detail-row">
                    <span class="detail-label">Price</span>
                    <span class="detail-value">${{ selected_product.price}}</span>
                </div>

                <form action="/delete" method="POST">
                    <input type="hidden" name="product_id" value="{{ selected_product.id }}">
                    <button type="submit" class="btn-delete">Delete Product</button>
                </form>

            {% else %}
                <div class="empty-state">
                    <div class="empty-icon">&#9783;</div>
                    <p>Select a product to see details</p>
                </div>
            {% endif %}
        </div>

    </div>
</body>
</html>